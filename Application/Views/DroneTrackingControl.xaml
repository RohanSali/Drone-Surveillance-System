<UserControl x:Class="DroneSurveillanceSystem.Views.DroneTrackingControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">

    <UserControl.Resources>
        <!-- Styles for the embedded control -->
        <Style x:Key="EmbeddedHeaderStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="5"/>
        </Style>

        <Style x:Key="EmbeddedDataTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#FFCCCCCC"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <Style x:Key="EmbeddedValueTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="2"/>
        </Style>

        <Style x:Key="GridLineStyle" TargetType="Line">
            <Setter Property="Stroke" Value="#FF333333"/>
            <Setter Property="StrokeThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#FF1A1A1A">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Map Canvas Section -->
        <Canvas Grid.Row="0" Name="MapCanvas" Background="#FF0F0F0F" ClipToBounds="True">
            <!-- Grid and drone indicators will be drawn programmatically -->
        </Canvas>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="#FF2D2D30" Padding="8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Active Drones:" Style="{StaticResource EmbeddedDataTextStyle}" VerticalAlignment="Center"/>
                    <TextBlock Name="ActiveDronesText" Text="{Binding ActiveDronesCount}" 
                              Foreground="#FF00FF00" FontWeight="Bold" FontSize="12" Margin="5,0" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="Status:" Style="{StaticResource EmbeddedDataTextStyle}" VerticalAlignment="Center"/>
                    <TextBlock Name="TrackingStatusText" Text="{Binding TrackingStatus}" 
                              Foreground="#FF00FF00" FontWeight="Bold" FontSize="12" Margin="5,0" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="Last Update:" Style="{StaticResource EmbeddedDataTextStyle}" VerticalAlignment="Center"/>
                    <TextBlock Name="LastUpdateText" Text="{Binding LastUpdateTime}" 
                              Style="{StaticResource EmbeddedValueTextStyle}" VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
