<Window x:Class="DroneSurveillanceSystem.Views.DroneDetailsPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Drone Details - Module Installation" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#0a0a0a"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="DarkButton" TargetType="Button">
            <Setter Property="Background" Value="#0078d4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        
        <Style x:Key="RemoveButton" TargetType="Button">
            <Setter Property="Background" Value="#dc3545"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="OptionButton" TargetType="Button">
            <Setter Property="Background" Value="#28a745"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70" />
            <RowDefinition Height="*" />
            <RowDefinition Height="60" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="#2d2d2d" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <TextBlock Text="🚁" FontSize="24" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBlock x:Name="DroneNameHeader" Text="Drone Details" 
                              Foreground="White" 
                              FontWeight="Bold" 
                              FontSize="24" 
                              VerticalAlignment="Center"/>
                </StackPanel>
                
                <Button Grid.Column="1" Name="BackToDronesListButton" Content="← Back to List" 
                       Style="{StaticResource DarkButton}" 
                       Click="BackToDronesListButton_Click" 
                       Margin="20,0"/>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="40">
            <Border Background="#2d2d2d" CornerRadius="12" Padding="30">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    
                    <!-- Drone Icon -->
                    <TextBlock Text="🚁" FontSize="80" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    
                    <!-- Drone Name -->
                    <TextBlock x:Name="DroneNameDisplay" Text="Drone_Alpha_1" 
                              Foreground="White" 
                              FontWeight="Bold" 
                              FontSize="28" 
                              HorizontalAlignment="Center" 
                              Margin="0,0,0,10"/>
                    
                    <!-- Connection Status -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
                        <Ellipse Width="16" Height="16" Fill="#4CAF50" Margin="0,0,10,0"/>
                        <TextBlock Text="USB Connected - Ready for Module Installation" 
                                  Foreground="#4CAF50" 
                                  FontSize="14"/>
                    </StackPanel>
                    
                    <!-- USB Port Info -->
                    <TextBlock x:Name="UsbPortInfo" Text="USB Port: COM3" 
                              Foreground="#ccc" 
                              FontSize="12" 
                              HorizontalAlignment="Center" 
                              Margin="0,0,0,30"/>
                    
                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button x:Name="ConnectButton" Content="🔗 Connect" 
                               Style="{StaticResource DarkButton}" 
                               Click="ConnectButton_Click"/>
                        <Button x:Name="RemoveButton" Content="🗑️ Remove" 
                               Style="{StaticResource RemoveButton}" 
                               Click="RemoveButton_Click"/>
                    </StackPanel>
                    
                    <!-- Connection Options (Initially Hidden) -->
                    <StackPanel x:Name="ConnectionOptionsPanel" 
                               Orientation="Horizontal" 
                               HorizontalAlignment="Center" 
                               Margin="0,20,0,0"
                               Visibility="Collapsed">
                        <TextBlock Text="Choose Action:" 
                                  Foreground="White" 
                                  FontSize="14" 
                                  VerticalAlignment="Center" 
                                  Margin="0,0,15,0"/>
                        <Button x:Name="FetchButton" Content="📥 Fetch" 
                               Style="{StaticResource OptionButton}" 
                               Click="FetchButton_Click"/>
                        <Button x:Name="InstallButton" Content="⚙️ Install" 
                               Style="{StaticResource OptionButton}" 
                               Click="InstallButton_Click"/>
                    </StackPanel>
                    
                </StackPanel>
            </Border>
        </Grid>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#2d2d2d">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="💡 Click Connect to access module installation options" 
                          Foreground="#ccc" 
                          FontSize="12"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
